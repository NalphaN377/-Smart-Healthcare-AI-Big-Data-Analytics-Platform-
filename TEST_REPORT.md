# 智慧医疗平台一期测试与验收报告

测试日期：2026-08-19  
测试范围：数据导入、SQL Server 存储、聚合分析、数据质量、AI 流式问答、报告生成、Vue 前端与二期接口预留。

运行环境：Python 3.11.9（项目 `.venv311`）。

## 1. 数据验收

| 指标 | 结果 |
|---|---:|
| 原始文件大小 | 832,373,138 Bytes |
| 原始行数 | 2,101,588 |
| 清洗后入库行数 | 2,094,418 |
| 去重/过滤行数 | 7,170 |
| 业务字段数 | 33 |
| 医疗机构数 | 205 |
| 全量导入耗时 | 292.07 秒 |
| 综合质量评分 | 99.98% |

质量维度：完整性 99.92%、准确性 100%、一致性 100%、时效性 100%、唯一性 99.66%。

## 2. 自动化测试

| 测试集 | 结果 |
|---|---|
| 数据清洗 | 3/3 通过 |
| 意图识别 | 3/3 通过 |
| 数据质量 | 2/2 通过 |
| 图表生成 | 2/2 通过 |
| REST/SSE API 契约 | 5/5 通过 |
| SQL Server 全量集成 | 1/1 通过 |
| 前端生产构建 | 通过，无编译错误 |

单元与 API 回归共 15 项全部通过；真实 SQL Server 集成测试验证了数据库名、精确行数、机构数、支付占比归一化、导入审计与质量结果。

## 3. 性能与端到端测试

项目书一期指标为普通聚合 API 不超过 3 秒、AI 问答不超过 30 秒。

| 场景 | 实测 |
|---|---:|
| 年龄段平均住院日 | 686.03 ms |
| 疾病费用 TOP 10 | 683.58 ms |
| 支付方式占比 | 306.03 ms |
| 年度趋势 | 588.99 ms |
| 服务区域通用聚合 | 576.55 ms |
| DeepSeek SSE 问答 | 5.86 秒，152 个 delta 事件 |
| AI 三章节报告 | 6.23 秒 |

所有被测场景满足一期性能指标。AI 流式响应包含 `context → delta → done` 完整事件序列。

## 4. 调试与修复记录

- 将不兼容本机 TLS 的 ODBC 默认链路替换为 `pymssql/FreeTDS`，保留 ODBC 可选后端。
- 将数据库连接超时和查询超时拆分，修复 210 万行冷缓存聚合被 10 秒误中断的问题。
- 总览的七组独立聚合并行执行，降低首屏等待时间。
- 原始 `Length of Stay = 120 +`、金额逗号/货币符号、负值、非新生儿出生体重均完成标准化。
- 所有动态维度和指标使用白名单，筛选值使用参数绑定，阻断 SQL 注入入口。
- SSE 增加外部模型失败的模板兜底；报告章节并行生成，避免三个模型调用串行等待。
- 高速导入增加“非空目标表拒绝重复导入”保护，必须显式 `--truncate` 才能全量重导。
- 前端删除演示期固定数字，改为 SQL Server 实时数据；修复长疾病名称挤压图表、离线状态、报告预览/下载和移动端布局。
- ECharts 按需引入并拆分生产包，前端构建不再产生大包告警。

## 5. 二期预留验收

以下能力保留稳定边界但一期不启用：Redis 缓存、增量 MERGE、备份恢复、费用预测、再入院风险、Spark/Hive 分布式计算、本地大模型、多轮对话与地图可视化。

- 配置层：`FEATURE_*` 与 `DISTRIBUTED_ENGINE` 开关。
- 数据层：staging 表、`incremental_update`、`backup`、`restore` 接口。
- 服务层：`/api/v2/cache|predictions|readmission-risk|backup|distributed|local-llm`，当前明确返回 HTTP 501。
- API 契约与前端一期功能保持解耦，二期实现时无需破坏现有接口。

## 6. 结论

一期核心功能、真实数据全量链路、前后端联调和 DeepSeek 流式调用均已通过验收。系统可用于项目演示与后续二期迭代；生产部署前仍需补充统一身份认证、HTTPS、审计留存、密钥托管和正式 WSGI/反向代理。
